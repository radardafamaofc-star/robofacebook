{
  "scripts": {
    "build:dev": "echo 'No build needed for Chrome extension'",
    "build": "echo 'No build needed for Chrome extension'"
  },
  "devDependencies": {
    "chrome-types": "^0.1.420"
  }
}
